sudo: false
language: ruby
cache: bundler

before_install:
  - gem install bundler
  - bundle --version
  - gem update --system
  - gem --version

matrix:
  include:
    - rvm: 2.3.4
    - rvm: 2.4.1
    - rvm: ruby-head
  allow_failures:
    - rvm: ruby-head
branches:
  only:
  - master

script:
  - bundle exec rake ci
